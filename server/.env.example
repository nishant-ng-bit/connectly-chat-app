# Server-only secrets. Configure these in your deployment provider, never in client env.
PORT=3001
CLIENT_URL=https://your-frontend-domain.example.com
# Use comma-separated origins when you have preview/prod frontend URLs.
# Example: CLIENT_URLS=https://connectly-ng.vercel.app,https://your-custom-domain.com
CLIENT_URLS=
# CORS_ORIGINS is also supported as an alias.
CORS_ORIGINS=
DATABASE_URL=mongodb+srv://USER:PASSWORD@HOST/DATABASE
JWT_SECRET=replace-with-a-long-random-secret

# Email/OTP
MAIL_USER=your-email@example.com
MAIL_PASS=your-app-password
MAIL_FROM=your-email@example.com
# Optional: use these for non-Gmail SMTP providers
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Cloudinary media uploads
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Optional AI features
GEMINI_API_KEY=your-gemini-api-key
